import { FormatNumberCommasPipe } from './format-number-commas.pipe';

describe('FormatNumberCommasPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNumberCommasPipe();
    expect(pipe).toBeTruthy();
  });
});
